# Backend (Express)

Simple Express setup.

Commands:

- `npm start` — run production server
- `npm run dev` — run with nodemon for development

Endpoints:
- `GET /` — health check
- `GET /api` — example API route
